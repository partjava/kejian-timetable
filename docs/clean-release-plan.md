# 空白课表版本实施计划

目标：按用户已确认的方案，首次安装只生成空学期，不预载个人课表；保留手动添加和导入。覆盖升级不主动删除用户数据。

- [x] 添加隐私资源检查，确认旧版失败；随后添加独立安装空白启动检查。
- [x] 删除 MainActivity 的预载逻辑；版本升级为 1.0.1（versionCode 2）。
- [x] 移除真实 JSON、XLS 和截图，导入测试改用虚构 CSV，更新说明。
- [x] 编译、服务测试、独立测试包首次启动验证；检查交付 APK 不含课表资源。
- [x] 生成新 APK，交付说明区分旧 Git 历史、GitHub Release 和手机数据。

执行方式：当前会话按 executing-plans 逐项执行，不自动提交或推送 Git，不改写历史，不删除下载目录原始文件。
