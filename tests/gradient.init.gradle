allprojects {
    afterEvaluate { project ->
        if (project.plugins.hasPlugin('com.android.application')) {
            project.android.defaultConfig.applicationId = 'com.kejian.gradientqa'
            project.android.defaultConfig.testInstrumentationRunner = 'com.kejian.app.GradientRegressionTest'
        }
    }
}
